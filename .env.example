# Database connection
# SQLite (development):
DATABASE_URL="file:./db/custom.db"
# MySQL (production):
# DATABASE_URL="mysql://USERNAME:PASSWORD@HOST:3306/jobready_dashboard"

# JWT secret for auth tokens (generate a strong random string for production)
JWT_SECRET="change-me-to-a-random-secret"

# Node environment
NODE_ENV="development"
